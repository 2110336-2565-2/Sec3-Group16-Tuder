// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/2110336-2565-2/Sec3-Group16-Tuder/ent/classcancelrequest"
	"github.com/2110336-2565-2/Sec3-Group16-Tuder/ent/match"
	"github.com/2110336-2565-2/Sec3-Group16-Tuder/ent/predicate"
	"github.com/2110336-2565-2/Sec3-Group16-Tuder/ent/user"
	"github.com/google/uuid"
)

// ClassCancelRequestQuery is the builder for querying ClassCancelRequest entities.
type ClassCancelRequestQuery struct {
	config
	ctx        *QueryContext
	order      []OrderFunc
	inters     []Interceptor
	predicates []predicate.ClassCancelRequest
	withMatch  *MatchQuery
	withUser   *UserQuery
	withFKs    bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ClassCancelRequestQuery builder.
func (ccrq *ClassCancelRequestQuery) Where(ps ...predicate.ClassCancelRequest) *ClassCancelRequestQuery {
	ccrq.predicates = append(ccrq.predicates, ps...)
	return ccrq
}

// Limit the number of records to be returned by this query.
func (ccrq *ClassCancelRequestQuery) Limit(limit int) *ClassCancelRequestQuery {
	ccrq.ctx.Limit = &limit
	return ccrq
}

// Offset to start from.
func (ccrq *ClassCancelRequestQuery) Offset(offset int) *ClassCancelRequestQuery {
	ccrq.ctx.Offset = &offset
	return ccrq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ccrq *ClassCancelRequestQuery) Unique(unique bool) *ClassCancelRequestQuery {
	ccrq.ctx.Unique = &unique
	return ccrq
}

// Order specifies how the records should be ordered.
func (ccrq *ClassCancelRequestQuery) Order(o ...OrderFunc) *ClassCancelRequestQuery {
	ccrq.order = append(ccrq.order, o...)
	return ccrq
}

// QueryMatch chains the current query on the "match" edge.
func (ccrq *ClassCancelRequestQuery) QueryMatch() *MatchQuery {
	query := (&MatchClient{config: ccrq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ccrq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ccrq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(classcancelrequest.Table, classcancelrequest.FieldID, selector),
			sqlgraph.To(match.Table, match.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, classcancelrequest.MatchTable, classcancelrequest.MatchColumn),
		)
		fromU = sqlgraph.SetNeighbors(ccrq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryUser chains the current query on the "user" edge.
func (ccrq *ClassCancelRequestQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: ccrq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ccrq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ccrq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(classcancelrequest.Table, classcancelrequest.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, classcancelrequest.UserTable, classcancelrequest.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(ccrq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ClassCancelRequest entity from the query.
// Returns a *NotFoundError when no ClassCancelRequest was found.
func (ccrq *ClassCancelRequestQuery) First(ctx context.Context) (*ClassCancelRequest, error) {
	nodes, err := ccrq.Limit(1).All(setContextOp(ctx, ccrq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{classcancelrequest.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ccrq *ClassCancelRequestQuery) FirstX(ctx context.Context) *ClassCancelRequest {
	node, err := ccrq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ClassCancelRequest ID from the query.
// Returns a *NotFoundError when no ClassCancelRequest ID was found.
func (ccrq *ClassCancelRequestQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = ccrq.Limit(1).IDs(setContextOp(ctx, ccrq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{classcancelrequest.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ccrq *ClassCancelRequestQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := ccrq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ClassCancelRequest entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ClassCancelRequest entity is found.
// Returns a *NotFoundError when no ClassCancelRequest entities are found.
func (ccrq *ClassCancelRequestQuery) Only(ctx context.Context) (*ClassCancelRequest, error) {
	nodes, err := ccrq.Limit(2).All(setContextOp(ctx, ccrq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{classcancelrequest.Label}
	default:
		return nil, &NotSingularError{classcancelrequest.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ccrq *ClassCancelRequestQuery) OnlyX(ctx context.Context) *ClassCancelRequest {
	node, err := ccrq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ClassCancelRequest ID in the query.
// Returns a *NotSingularError when more than one ClassCancelRequest ID is found.
// Returns a *NotFoundError when no entities are found.
func (ccrq *ClassCancelRequestQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = ccrq.Limit(2).IDs(setContextOp(ctx, ccrq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{classcancelrequest.Label}
	default:
		err = &NotSingularError{classcancelrequest.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ccrq *ClassCancelRequestQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := ccrq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ClassCancelRequests.
func (ccrq *ClassCancelRequestQuery) All(ctx context.Context) ([]*ClassCancelRequest, error) {
	ctx = setContextOp(ctx, ccrq.ctx, "All")
	if err := ccrq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ClassCancelRequest, *ClassCancelRequestQuery]()
	return withInterceptors[[]*ClassCancelRequest](ctx, ccrq, qr, ccrq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ccrq *ClassCancelRequestQuery) AllX(ctx context.Context) []*ClassCancelRequest {
	nodes, err := ccrq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ClassCancelRequest IDs.
func (ccrq *ClassCancelRequestQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if ccrq.ctx.Unique == nil && ccrq.path != nil {
		ccrq.Unique(true)
	}
	ctx = setContextOp(ctx, ccrq.ctx, "IDs")
	if err = ccrq.Select(classcancelrequest.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ccrq *ClassCancelRequestQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := ccrq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ccrq *ClassCancelRequestQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ccrq.ctx, "Count")
	if err := ccrq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ccrq, querierCount[*ClassCancelRequestQuery](), ccrq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ccrq *ClassCancelRequestQuery) CountX(ctx context.Context) int {
	count, err := ccrq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ccrq *ClassCancelRequestQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ccrq.ctx, "Exist")
	switch _, err := ccrq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ccrq *ClassCancelRequestQuery) ExistX(ctx context.Context) bool {
	exist, err := ccrq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ClassCancelRequestQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ccrq *ClassCancelRequestQuery) Clone() *ClassCancelRequestQuery {
	if ccrq == nil {
		return nil
	}
	return &ClassCancelRequestQuery{
		config:     ccrq.config,
		ctx:        ccrq.ctx.Clone(),
		order:      append([]OrderFunc{}, ccrq.order...),
		inters:     append([]Interceptor{}, ccrq.inters...),
		predicates: append([]predicate.ClassCancelRequest{}, ccrq.predicates...),
		withMatch:  ccrq.withMatch.Clone(),
		withUser:   ccrq.withUser.Clone(),
		// clone intermediate query.
		sql:  ccrq.sql.Clone(),
		path: ccrq.path,
	}
}

// WithMatch tells the query-builder to eager-load the nodes that are connected to
// the "match" edge. The optional arguments are used to configure the query builder of the edge.
func (ccrq *ClassCancelRequestQuery) WithMatch(opts ...func(*MatchQuery)) *ClassCancelRequestQuery {
	query := (&MatchClient{config: ccrq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ccrq.withMatch = query
	return ccrq
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (ccrq *ClassCancelRequestQuery) WithUser(opts ...func(*UserQuery)) *ClassCancelRequestQuery {
	query := (&UserClient{config: ccrq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ccrq.withUser = query
	return ccrq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Title string `json:"title,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ClassCancelRequest.Query().
//		GroupBy(classcancelrequest.FieldTitle).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ccrq *ClassCancelRequestQuery) GroupBy(field string, fields ...string) *ClassCancelRequestGroupBy {
	ccrq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ClassCancelRequestGroupBy{build: ccrq}
	grbuild.flds = &ccrq.ctx.Fields
	grbuild.label = classcancelrequest.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Title string `json:"title,omitempty"`
//	}
//
//	client.ClassCancelRequest.Query().
//		Select(classcancelrequest.FieldTitle).
//		Scan(ctx, &v)
func (ccrq *ClassCancelRequestQuery) Select(fields ...string) *ClassCancelRequestSelect {
	ccrq.ctx.Fields = append(ccrq.ctx.Fields, fields...)
	sbuild := &ClassCancelRequestSelect{ClassCancelRequestQuery: ccrq}
	sbuild.label = classcancelrequest.Label
	sbuild.flds, sbuild.scan = &ccrq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ClassCancelRequestSelect configured with the given aggregations.
func (ccrq *ClassCancelRequestQuery) Aggregate(fns ...AggregateFunc) *ClassCancelRequestSelect {
	return ccrq.Select().Aggregate(fns...)
}

func (ccrq *ClassCancelRequestQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ccrq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ccrq); err != nil {
				return err
			}
		}
	}
	for _, f := range ccrq.ctx.Fields {
		if !classcancelrequest.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ccrq.path != nil {
		prev, err := ccrq.path(ctx)
		if err != nil {
			return err
		}
		ccrq.sql = prev
	}
	return nil
}

func (ccrq *ClassCancelRequestQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ClassCancelRequest, error) {
	var (
		nodes       = []*ClassCancelRequest{}
		withFKs     = ccrq.withFKs
		_spec       = ccrq.querySpec()
		loadedTypes = [2]bool{
			ccrq.withMatch != nil,
			ccrq.withUser != nil,
		}
	)
	if ccrq.withMatch != nil || ccrq.withUser != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, classcancelrequest.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ClassCancelRequest).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ClassCancelRequest{config: ccrq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ccrq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ccrq.withMatch; query != nil {
		if err := ccrq.loadMatch(ctx, query, nodes, nil,
			func(n *ClassCancelRequest, e *Match) { n.Edges.Match = e }); err != nil {
			return nil, err
		}
	}
	if query := ccrq.withUser; query != nil {
		if err := ccrq.loadUser(ctx, query, nodes, nil,
			func(n *ClassCancelRequest, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ccrq *ClassCancelRequestQuery) loadMatch(ctx context.Context, query *MatchQuery, nodes []*ClassCancelRequest, init func(*ClassCancelRequest), assign func(*ClassCancelRequest, *Match)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ClassCancelRequest)
	for i := range nodes {
		if nodes[i].match_class_cancel_request == nil {
			continue
		}
		fk := *nodes[i].match_class_cancel_request
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(match.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "match_class_cancel_request" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (ccrq *ClassCancelRequestQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*ClassCancelRequest, init func(*ClassCancelRequest), assign func(*ClassCancelRequest, *User)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ClassCancelRequest)
	for i := range nodes {
		if nodes[i].user_class_cancel_request == nil {
			continue
		}
		fk := *nodes[i].user_class_cancel_request
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_class_cancel_request" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (ccrq *ClassCancelRequestQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ccrq.querySpec()
	_spec.Node.Columns = ccrq.ctx.Fields
	if len(ccrq.ctx.Fields) > 0 {
		_spec.Unique = ccrq.ctx.Unique != nil && *ccrq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ccrq.driver, _spec)
}

func (ccrq *ClassCancelRequestQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(classcancelrequest.Table, classcancelrequest.Columns, sqlgraph.NewFieldSpec(classcancelrequest.FieldID, field.TypeUUID))
	_spec.From = ccrq.sql
	if unique := ccrq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ccrq.path != nil {
		_spec.Unique = true
	}
	if fields := ccrq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, classcancelrequest.FieldID)
		for i := range fields {
			if fields[i] != classcancelrequest.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ccrq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ccrq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ccrq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ccrq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ccrq *ClassCancelRequestQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ccrq.driver.Dialect())
	t1 := builder.Table(classcancelrequest.Table)
	columns := ccrq.ctx.Fields
	if len(columns) == 0 {
		columns = classcancelrequest.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ccrq.sql != nil {
		selector = ccrq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ccrq.ctx.Unique != nil && *ccrq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ccrq.predicates {
		p(selector)
	}
	for _, p := range ccrq.order {
		p(selector)
	}
	if offset := ccrq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ccrq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ClassCancelRequestGroupBy is the group-by builder for ClassCancelRequest entities.
type ClassCancelRequestGroupBy struct {
	selector
	build *ClassCancelRequestQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ccrgb *ClassCancelRequestGroupBy) Aggregate(fns ...AggregateFunc) *ClassCancelRequestGroupBy {
	ccrgb.fns = append(ccrgb.fns, fns...)
	return ccrgb
}

// Scan applies the selector query and scans the result into the given value.
func (ccrgb *ClassCancelRequestGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ccrgb.build.ctx, "GroupBy")
	if err := ccrgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ClassCancelRequestQuery, *ClassCancelRequestGroupBy](ctx, ccrgb.build, ccrgb, ccrgb.build.inters, v)
}

func (ccrgb *ClassCancelRequestGroupBy) sqlScan(ctx context.Context, root *ClassCancelRequestQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ccrgb.fns))
	for _, fn := range ccrgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ccrgb.flds)+len(ccrgb.fns))
		for _, f := range *ccrgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ccrgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ccrgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ClassCancelRequestSelect is the builder for selecting fields of ClassCancelRequest entities.
type ClassCancelRequestSelect struct {
	*ClassCancelRequestQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ccrs *ClassCancelRequestSelect) Aggregate(fns ...AggregateFunc) *ClassCancelRequestSelect {
	ccrs.fns = append(ccrs.fns, fns...)
	return ccrs
}

// Scan applies the selector query and scans the result into the given value.
func (ccrs *ClassCancelRequestSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ccrs.ctx, "Select")
	if err := ccrs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ClassCancelRequestQuery, *ClassCancelRequestSelect](ctx, ccrs.ClassCancelRequestQuery, ccrs, ccrs.inters, v)
}

func (ccrs *ClassCancelRequestSelect) sqlScan(ctx context.Context, root *ClassCancelRequestQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ccrs.fns))
	for _, fn := range ccrs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ccrs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ccrs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
