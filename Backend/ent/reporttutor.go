// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent/dialect/sql"
	"github.com/2110336-2565-2/Sec3-Group16-Tuder/ent/reporttutor"
)

// ReportTutor is the model entity for the ReportTutor schema.
type ReportTutor struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Score holds the value of the "score" field.
	Score *float32 `json:"score,omitempty"`
	// ReviewMsg holds the value of the "review_msg" field.
	ReviewMsg *string `json:"review_msg,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ReportTutor) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case reporttutor.FieldScore:
			values[i] = new(sql.NullFloat64)
		case reporttutor.FieldID:
			values[i] = new(sql.NullInt64)
		case reporttutor.FieldReviewMsg:
			values[i] = new(sql.NullString)
		default:
			return nil, fmt.Errorf("unexpected column %q for type ReportTutor", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ReportTutor fields.
func (rt *ReportTutor) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case reporttutor.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			rt.ID = int(value.Int64)
		case reporttutor.FieldScore:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field score", values[i])
			} else if value.Valid {
				rt.Score = new(float32)
				*rt.Score = float32(value.Float64)
			}
		case reporttutor.FieldReviewMsg:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field review_msg", values[i])
			} else if value.Valid {
				rt.ReviewMsg = new(string)
				*rt.ReviewMsg = value.String
			}
		}
	}
	return nil
}

// Update returns a builder for updating this ReportTutor.
// Note that you need to call ReportTutor.Unwrap() before calling this method if this ReportTutor
// was returned from a transaction, and the transaction was committed or rolled back.
func (rt *ReportTutor) Update() *ReportTutorUpdateOne {
	return NewReportTutorClient(rt.config).UpdateOne(rt)
}

// Unwrap unwraps the ReportTutor entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (rt *ReportTutor) Unwrap() *ReportTutor {
	_tx, ok := rt.config.driver.(*txDriver)
	if !ok {
		panic("ent: ReportTutor is not a transactional entity")
	}
	rt.config.driver = _tx.drv
	return rt
}

// String implements the fmt.Stringer.
func (rt *ReportTutor) String() string {
	var builder strings.Builder
	builder.WriteString("ReportTutor(")
	builder.WriteString(fmt.Sprintf("id=%v, ", rt.ID))
	if v := rt.Score; v != nil {
		builder.WriteString("score=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	if v := rt.ReviewMsg; v != nil {
		builder.WriteString("review_msg=")
		builder.WriteString(*v)
	}
	builder.WriteByte(')')
	return builder.String()
}

// ReportTutors is a parsable slice of ReportTutor.
type ReportTutors []*ReportTutor
